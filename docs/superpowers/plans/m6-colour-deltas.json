{
  "_README": [
    "M6 colour-delta allowlist. Consumed as:",
    "  node <tools>/compare-colours.mjs docs/superpowers/plans/m6-colour-baseline.json <captured.json> --allow docs/superpowers/plans/m6-colour-deltas.json",
    "Created by M6 T3 under Ruling A. The baseline (m6-colour-baseline.json, captured at 5de16ee before any M6 edit) is frozen and must never be modified; this file is the disclosed, ruled set of changes on top of it, so T13's headline gate -- 'nothing changed except the disclosed deltas' -- stays checkable.",
    "THEME KEYS ONLY ARE READ. compare-colours.mjs treats every top-level key as a theme; keys beginning with '_' are skipped as prose. That skip was ADDED BY M6 T3 to the stale-entry loop (it previously existed only in themes()); without it a '_disclosures' key became a phantom theme and every member was reported 'STALE ALLOWLIST ENTRY'. The harness is gitignored, so T13 inherits that edit -- if compare-colours.mjs is ever restored from an older copy, re-apply it or move these notes out.",
    "VALUES ARE COMPUTED STRINGS, NOT HEX. The comparator does an exact string compare against getComputedStyle output, so an entry must read 'rgb(120, 53, 15)', not '#78350F'.",
    "A STALE ENTRY FAILS. An entry naming a token that did not actually change is reported and exits 1, so this file cannot silently over-permit."
  ],
  "_disclosures": {
    "light --hl-ink": {
      "theme": "light",
      "token": "--hl-ink",
      "before": "rgb(16, 19, 34)",
      "after": "rgb(120, 53, 15)",
      "before_hex": "#101322",
      "after_hex": "#78350F",
      "decision": "M6 T3 step 5",
      "reason": "The light --hl-ink was byte-identical to --ink, so the search-match highlight declared an ink it never actually changed. (The declaration was not dead -- Chrome's UA sheet gives `mark` color rgb(0,0,0) -- but it was visually indistinguishable from inherited body text, so it read as a chosen value when no choice had been made.) #78350F is amber-900 against --hl-bg's amber-100 and is coherent with the amber-700 --warning-text added in the same task. MEASURED in Chrome: 8.15:1 on --hl-bg, versus --ink's 16.57:1. Consumed by `.palette-snippet mark` (CommandPalette.css:186) and `.message-search-result-text mark` (MessageSearch.css:248).",
      "dark_side": "UNCHANGED at rgb(253, 243, 220). Board 2d gives no dark highlight pair at all, so the dark values are interpolations with nothing to migrate toward; M6 T3 kept them BY DECISION and deleted their `refine in M6` markers rather than inventing a board value in the final milestone."
    }
  },
  "_changes_that_cannot_appear_as_drift": {
    "_note": "probe-colour-tokens.js snapshots a frozen list of the 81 canonical :root tokens. Tokens ADDED by M6 T3 are not on that list, so they can never show as drift and need no allowlist entry. Recorded here so the task's full colour surface is auditable from one place.",
    "--stage-accent": "#6366F1 -- NEW on :root. Decision 2: the stage is dark in BOTH themes, so its accent is pinned to the dark value. Replaces var(--accent) at 9 CallStage.css sites.",
    "--stage-accent-hover": "#818CF8 -- NEW on :root. CORRECTION TO THE PLAN by M6 T3: the plan routed .stage-share-banner-btn:hover (CallStage.css:604) to --stage-accent, but CallStage.css:595 is that button's REST state, so both would have become the same colour and the hover affordance would have been silently deleted. #818CF8 is the dark theme's --accent-hover, pinned exactly as --stage-accent is the dark theme's --accent.",
    "--stage-danger": "#FCA5A5 -- NEW on :root. Decision 6. Replaces var(--danger) at 3 sites (.stage-plate-mic.is-muted, .stage-conn.is-poor, .p2p-plate-mic.is-muted). .stage-tip.is-poor deliberately KEEPS --danger: it paints var(--canvas) and is theme-adaptive.",
    "_site_reroutes": "Two SITES changed which token they read (no token value changed, so the 81-token gate cannot see these either). Cited by SELECTOR, not line number -- each site's own explanatory comment pushed its line out from under an earlier line-number citation here. .voice-participant-mic.off (ChannelSidebar.css) and .msg-mention-everyone (MessageRow.css) moved from --danger to --danger-text: on their real composited dark grounds --danger computed 3.92:1 and 4.22:1, below AA. Now 8.15:1 and 8.76:1 dark, 5.22:1 both in light. Ground values are browser-resolved but composited arithmetically (--accent-soft is translucent), so these are computed ratios, not read pixels. Human ruling 2026-08-30. This is why the --danger census in tokens.css drops from 5 foregrounds to 3: the three that remain (.context-menu-item.is-danger, .settings-nav-logout, .stage-tip.is-poor) pass on dark ground at 4.82 / 4.54 / 4.82:1 and were left alone. --danger itself takes no dark override -- see the comment beside it in tokens.css.",
    "--warning-text": "#B45309 on :root, #F59E0B under [data-theme=\"dark\"] -- NEW. M6 T3 step 4. Light .setting-warning was 2.01:1; it is 4.69:1 now. The dark override is a CORRECTION TO THE PLAN, which said none was needed: true of --warning itself (8.16:1 on dark --canvas-2) but not of the plan's own remedy, since once `color` reads --warning-text the dark theme reads it too and the :root value measures only 3.49:1 there.",

    "_T13_NOTE": "M6 T13 deleted the 47 legacy aliases. NONE of the 47 is among the frozen 81, so the whole deletion is invisible to the token gate -- which ran PASS on the shipped tree, both themes, with only T3's --hl-ink entry used. T13's real deltas are all ELEMENT-level and are listed below. They were measured with tools/probe-t13-aliases.js across (light|dark) x (1440|760) before and after, and the reorder half was measured separately (mid -> after: 0 changed keys of 168, in all four runs).",
    "--accent-300": "#A5B4FC -- NEW on :root, theme-invariant. Decision 15. Replaces the legacy :root-only --brand-300 at ProfileSettings.css `.profile-avatar-large`. Same literal, so the gradient is unchanged: linear-gradient(135deg, rgb(165,180,252), rgb(99,102,241)) in BOTH themes, before and after.",
    "--accent-400": "#818CF8 -- NEW on :root, theme-invariant. Decision 15. Replaces the legacy --brand-400 at Auth.css `.auth-card::before`. Same literal; measured unchanged in both themes.",
    "--accent-500": "#6366F1 -- NEW on :root, theme-invariant. Decision 15. Replaces the legacy --brand-500 at ProfileSettings.css, and is also the base of the two color-mix() tints that replaced Auth's raw rgb() glows. #6366F1 is the DARK theme's --accent, which is why it is pinned :root-only exactly as --stage-accent is; pointing it at --accent would change light theme from #4F46E5.",
    "_T13_element_deltas": {
      "_note": "Four rendered changes. Three are decisions 13/14; the fourth is the one colour change T13 introduced that no decision named, and it is disclosed here rather than absorbed.",
      "decision 13 -- --radius-lg 12px -> --radius-tile 13px": "+1px on THREE square-tile surfaces. MEASURED at the element: ServerList.css `.server-icon-symbol` and `.server-icon img` (both inside @media (width <= 768px), so 12px -> 13px at a 760px viewport and untouched at 1440 -- the desktop reads are 0px and 14px), and Auth.css `.auth-card::before` (12px -> 13px at every width). The canonical radius scale is 6/9/10/11/13/14/16/18/999 and has no 12px step; adding one would have preserved a legacy value inside the design system in the milestone whose purpose is removing legacy values.",
      "decision 14 -- --brand-subtle -> --focus-ring": "Auth.css `.form-group input:focus`. The WHOLE declaration moved, not just the colour. DISCLOSED ALPHA CHANGE, measured with protocol-level :focus (CDP CSS.forcePseudoState -- el.focus() does not make :focus match in the headless harness): light rgba(79,70,229,0.1) -> rgba(79,70,229,0.13); dark rgba(99,102,241,0.15) -> rgba(99,102,241,0.25). Geometry (0 0 0 3px) and hue unchanged in both themes. --brand-subtle was the ONLY alias whose [data-theme=\"dark\"] override anything consumed, and --focus-ring is the only canonical token that carries its own.",
      "spec 9.4 -- Auth.css `.auth-error` border": "THE ONE UNDECIDED COLOUR CHANGE IN T13. The border was a raw `rgb(239 68 68 / 15%)`. Unlike the two glows on the same page, #EF4444 is Tailwind red-500 and is reproduced by NO token in this system (--danger is #E7444A), so there was no byte-identical replacement. Measured at the element: rgba(239,68,68,0.15) -> rgba(231,68,74,0.15), both themes. It is a 15%-alpha hairline over --danger-soft, and it moves the border onto the token the fill and the text already read.",
      "spec 9.4 -- the three BYTE-IDENTICAL raw-value replacements": "Auth.css `.auth-btn:hover` box-shadow and `.auth-card::before` box-shadow moved from raw rgb(99 102 241 / 35% | 30%) to color-mix(in srgb, var(--accent-500) N%, transparent); TitleBar.css `.title-bar button.title-bar-close:hover` moved from `#FFF` to var(--white). The computed-value STRING changes for the two color-mix sites -- Chrome serialises the result in the mixing space, so rgba(99,102,241,0.35) prints as color(srgb 0.388235 0.4 0.945098 / 0.35) -- but the PAINTED pixel does not. Verified with tools/probe-t13-colormix.js on two reads (composited over opaque white, and un-premultiplied on a transparent canvas), each with its own MEASURED sensitivity floor: both instruments separate a 3/255 RGB nudge and a 1% alpha nudge, neither separates a 1/255 RGB nudge, and 0.388235*255 = 99.000 exactly closes the remaining gap arithmetically. --white is #FFF on :root with no dark override, so TitleBar is identical by inspection and by measurement."
    }
  },
  "light": {
    "--hl-ink": "rgb(120, 53, 15)"
  }
}
