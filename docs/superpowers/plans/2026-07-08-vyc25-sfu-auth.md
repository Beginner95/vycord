# VYC-25: JWT-аутентификация на SFU `/ws` — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SFU принимает WebSocket-подключения на `/ws` только с валидным JWT; идентичность пользователя берётся из claims токена, а не из query.

**Architecture:** Общий пакет `server/pkg/authtoken` со stateless-валидацией JWT (HMAC + `exp` + `user_id`-claim), переиспользуемый auth-usecase'ом API и хендлером SFU. Клиент передаёт токен из localStorage в query `?token=` (паттерн чатового WS), перечитывая его на каждой попытке подключения.

**Tech Stack:** Go 1.x (`github.com/vycord/server`), `golang-jwt/jwt/v5`, `google/uuid`, `gorilla/websocket`, `joho/godotenv`; клиент — TypeScript/Vite; e2e — bash + headless Chrome + WebCrypto.

**Спека:** `docs/superpowers/specs/2026-07-08-vyc25-sfu-auth-design.md`

## Global Constraints

- Env-переменная секрета: `JWT_SECRET` (общая для API и SFU; в prod SFU уже получает её через `env_file: .env.prod`, деплой не меняется).
- Ключ токена в localStorage клиента: `vycord_token`.
- Метод подписи: только HMAC (`SigningMethodHMAC`); `alg=none` и другие методы отвергаются.
- Claims: `user_id` — строка с UUID; отсутствие/невалидность → ошибка.
- Ответы хендлера до upgrade: нет токена → `401`; нет `room_id` → `400`; невалидный/истёкший токен → `401` + `Warn`-лог без содержимого токена.
- Никакого `user_id` в query и в `JoinPayload` — единственный источник идентичности это токен.
- Все Go-команды выполняются из `/www/my/vycord/server`; клиентские — из `/www/my/vycord/client`.
- Сообщения коммитов — на русском, с префиксом `VYC-25`, футер `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Пакет `server/pkg/authtoken`

**Files:**
- Create: `server/pkg/authtoken/authtoken.go`
- Test: `server/pkg/authtoken/authtoken_test.go`

**Interfaces:**
- Consumes: ничего (только `jwt/v5`, `uuid`).
- Produces: `func ValidateToken(secret, tokenString string) (uuid.UUID, error)` — используется в Task 2 (auth-usecase) и Task 3 (SFU-хендлер).

- [ ] **Step 1: Написать падающие тесты**

Создать `server/pkg/authtoken/authtoken_test.go`:

```go
package authtoken

import (
	"testing"
	"time"

	"github.com/golang-jwt/jwt/v5"
	"github.com/google/uuid"
)

const secret = "unit-test-secret"

func sign(t *testing.T, method jwt.SigningMethod, key any, claims jwt.MapClaims) string {
	t.Helper()
	s, err := jwt.NewWithClaims(method, claims).SignedString(key)
	if err != nil {
		t.Fatalf("sign: %v", err)
	}
	return s
}

func hs256(t *testing.T, key string, claims jwt.MapClaims) string {
	t.Helper()
	return sign(t, jwt.SigningMethodHS256, []byte(key), claims)
}

func TestValidToken(t *testing.T) {
	want := uuid.New()
	tok := hs256(t, secret, jwt.MapClaims{
		"user_id": want.String(),
		"exp":     time.Now().Add(time.Hour).Unix(),
	})

	got, err := ValidateToken(secret, tok)
	if err != nil {
		t.Fatalf("ValidateToken: %v", err)
	}
	if got != want {
		t.Fatalf("user id = %s, want %s", got, want)
	}
}

func TestExpiredToken(t *testing.T) {
	tok := hs256(t, secret, jwt.MapClaims{
		"user_id": uuid.NewString(),
		"exp":     time.Now().Add(-time.Hour).Unix(),
	})
	if _, err := ValidateToken(secret, tok); err == nil {
		t.Fatal("expired token accepted")
	}
}

func TestWrongSignature(t *testing.T) {
	tok := hs256(t, "another-secret", jwt.MapClaims{
		"user_id": uuid.NewString(),
		"exp":     time.Now().Add(time.Hour).Unix(),
	})
	if _, err := ValidateToken(secret, tok); err == nil {
		t.Fatal("token signed with a different secret accepted")
	}
}

func TestAlgNone(t *testing.T) {
	tok := sign(t, jwt.SigningMethodNone, jwt.UnsafeAllowNoneSignatureType, jwt.MapClaims{
		"user_id": uuid.NewString(),
		"exp":     time.Now().Add(time.Hour).Unix(),
	})
	if _, err := ValidateToken(secret, tok); err == nil {
		t.Fatal("alg=none token accepted")
	}
}

func TestMissingUserID(t *testing.T) {
	tok := hs256(t, secret, jwt.MapClaims{
		"exp": time.Now().Add(time.Hour).Unix(),
	})
	if _, err := ValidateToken(secret, tok); err == nil {
		t.Fatal("token without user_id accepted")
	}
}

func TestNonStringUserID(t *testing.T) {
	tok := hs256(t, secret, jwt.MapClaims{
		"user_id": 42,
		"exp":     time.Now().Add(time.Hour).Unix(),
	})
	if _, err := ValidateToken(secret, tok); err == nil {
		t.Fatal("token with non-string user_id accepted")
	}
}

func TestNonUUIDUserID(t *testing.T) {
	tok := hs256(t, secret, jwt.MapClaims{
		"user_id": "not-a-uuid",
		"exp":     time.Now().Add(time.Hour).Unix(),
	})
	if _, err := ValidateToken(secret, tok); err == nil {
		t.Fatal("token with non-UUID user_id accepted")
	}
}
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `cd /www/my/vycord/server && go test ./pkg/authtoken/`
Expected: FAIL — `undefined: ValidateToken` (пакет не компилируется).

- [ ] **Step 3: Реализация**

Создать `server/pkg/authtoken/authtoken.go`. Логика извлечена из `internal/usecase/auth.go:94` (`ValidateToken`), но небезопасный type assertion `claims["user_id"].(string)` заменён на проверяемый (в оригинале — паника при отсутствии claim):

```go
// Package authtoken validates the JWT tokens issued by the API's auth
// use-case. It is shared by every service that needs to check a token
// against the common JWT_SECRET without a database round-trip (API
// middleware, SFU signaling).
package authtoken

import (
	"fmt"

	"github.com/golang-jwt/jwt/v5"
	"github.com/google/uuid"
)

// ValidateToken parses tokenString, verifies its HMAC signature and standard
// claims (including exp) against secret, and returns the user ID from the
// user_id claim. Only HMAC signing methods are accepted — anything else,
// including alg=none, is rejected.
func ValidateToken(secret, tokenString string) (uuid.UUID, error) {
	token, err := jwt.Parse(tokenString, func(token *jwt.Token) (interface{}, error) {
		if _, ok := token.Method.(*jwt.SigningMethodHMAC); !ok {
			return nil, fmt.Errorf("unexpected signing method: %v", token.Header["alg"])
		}
		return []byte(secret), nil
	})
	if err != nil {
		return uuid.Nil, fmt.Errorf("invalid token: %w", err)
	}

	claims, ok := token.Claims.(jwt.MapClaims)
	if !ok || !token.Valid {
		return uuid.Nil, fmt.Errorf("invalid token claims")
	}

	rawID, ok := claims["user_id"].(string)
	if !ok {
		return uuid.Nil, fmt.Errorf("missing or invalid user_id claim")
	}
	userID, err := uuid.Parse(rawID)
	if err != nil {
		return uuid.Nil, fmt.Errorf("invalid user id in token")
	}
	return userID, nil
}
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `cd /www/my/vycord/server && go test ./pkg/authtoken/ -v`
Expected: PASS, все 7 тестов.

- [ ] **Step 5: Commit**

```bash
git add server/pkg/authtoken/
git commit -m "$(cat <<'EOF'
VYC-25 Пакет authtoken: stateless-валидация JWT общим секретом

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Рефакторинг auth-usecase на `pkg/authtoken`

**Files:**
- Modify: `server/internal/usecase/auth.go:94-122` (метод `ValidateToken`)

**Interfaces:**
- Consumes: `authtoken.ValidateToken(secret, tokenString string) (uuid.UUID, error)` из Task 1.
- Produces: сигнатура `authUseCase.ValidateToken(tokenString string) (*domain.User, error)` не меняется — вызывающие (`middleware/auth.go`, `handler/websocket.go`) не трогаются.

- [ ] **Step 1: Заменить тело метода**

В `server/internal/usecase/auth.go` заменить весь метод `ValidateToken` (строки 94–122) на:

```go
func (uc *authUseCase) ValidateToken(tokenString string) (*domain.User, error) {
	userID, err := authtoken.ValidateToken(uc.jwtSecret, tokenString)
	if err != nil {
		return nil, err
	}

	user, err := uc.userRepo.GetByID(userID)
	if err != nil {
		return nil, fmt.Errorf("user not found: %w", err)
	}

	return user, nil
}
```

В импорты файла добавить `"github.com/vycord/server/pkg/authtoken"`; импорт `"github.com/golang-jwt/jwt/v5"` остаётся — он нужен `generateToken`.

- [ ] **Step 2: Проверить сборку и все тесты**

Run: `cd /www/my/vycord/server && go build ./... && go vet ./... && go test ./...`
Expected: сборка ок, все тесты PASS (парсинг покрыт тестами Task 1; у usecase своих тестов нет).

- [ ] **Step 3: Commit**

```bash
git add server/internal/usecase/auth.go
git commit -m "$(cat <<'EOF'
VYC-25 Auth-usecase парсит JWT через pkg/authtoken

Одна реализация парсинга вместо двух расходящихся копий; заодно
устранена паника на токене без user_id-claim (был небезопасный
type assertion).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Аутентификация в SFU-хендлере

**Files:**
- Modify: `server/internal/sfu/transport/signaling/handler.go` (`NewHandler`, `ServeHTTP`)
- Modify: `server/internal/sfu/transport/signaling/protocol.go:11-16` (удалить `JoinPayload`)
- Modify: `server/cmd/sfu/main.go` (загрузка `.env`, чтение `JWT_SECRET`, fail-fast)
- Test: `server/internal/sfu/transport/signaling/handler_test.go`

**Interfaces:**
- Consumes: `authtoken.ValidateToken(secret, tokenString string) (uuid.UUID, error)` из Task 1.
- Produces: `signaling.NewHandler(manager *application.RoomManager, log *slog.Logger, jwtSecret string) *Handler` — новая сигнатура, используется в `cmd/sfu/main.go` (в этом же таске) и косвенно в e2e (Task 5: SFU требует `JWT_SECRET` при старте).

- [ ] **Step 1: Написать падающие тесты хендлера**

Создать `server/internal/sfu/transport/signaling/handler_test.go`:

```go
package signaling

import (
	"io"
	"log/slog"
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"
	"time"

	"github.com/golang-jwt/jwt/v5"
	"github.com/google/uuid"
	"github.com/gorilla/websocket"

	"github.com/vycord/server/internal/sfu/application"
	sfuwebrtc "github.com/vycord/server/internal/sfu/infrastructure/webrtc"
)

const testSecret = "handler-test-secret"

func newTestServer(t *testing.T) *httptest.Server {
	t.Helper()
	log := slog.New(slog.NewTextHandler(io.Discard, nil))
	pf, err := sfuwebrtc.NewPeerFactory([]string{}, "")
	if err != nil {
		t.Fatalf("NewPeerFactory: %v", err)
	}
	mgr := application.NewRoomManager(pf, log)
	t.Cleanup(mgr.Shutdown)
	srv := httptest.NewServer(NewHandler(mgr, log, testSecret))
	t.Cleanup(srv.Close)
	return srv
}

func signToken(t *testing.T, userID string, exp time.Time) string {
	t.Helper()
	tok := jwt.NewWithClaims(jwt.SigningMethodHS256, jwt.MapClaims{
		"user_id": userID,
		"exp":     exp.Unix(),
	})
	s, err := tok.SignedString([]byte(testSecret))
	if err != nil {
		t.Fatalf("sign token: %v", err)
	}
	return s
}

// dialStatus attempts a WebSocket handshake and returns the HTTP status of
// the response (101 on a successful upgrade).
func dialStatus(t *testing.T, srv *httptest.Server, query string) int {
	t.Helper()
	url := "ws" + strings.TrimPrefix(srv.URL, "http") + "/ws?" + query
	conn, resp, err := websocket.DefaultDialer.Dial(url, nil)
	if err == nil {
		conn.Close()
	}
	if resp == nil {
		t.Fatalf("dial returned no HTTP response: %v", err)
	}
	return resp.StatusCode
}

func TestServeHTTPRejectsMissingToken(t *testing.T) {
	srv := newTestServer(t)
	if got := dialStatus(t, srv, "room_id=r1"); got != http.StatusUnauthorized {
		t.Fatalf("status = %d, want 401", got)
	}
}

func TestServeHTTPRejectsMissingRoomID(t *testing.T) {
	srv := newTestServer(t)
	tok := signToken(t, uuid.NewString(), time.Now().Add(time.Hour))
	if got := dialStatus(t, srv, "token="+tok); got != http.StatusBadRequest {
		t.Fatalf("status = %d, want 400", got)
	}
}

func TestServeHTTPRejectsGarbageToken(t *testing.T) {
	srv := newTestServer(t)
	if got := dialStatus(t, srv, "room_id=r1&token=garbage"); got != http.StatusUnauthorized {
		t.Fatalf("status = %d, want 401", got)
	}
}

func TestServeHTTPRejectsExpiredToken(t *testing.T) {
	srv := newTestServer(t)
	tok := signToken(t, uuid.NewString(), time.Now().Add(-time.Hour))
	if got := dialStatus(t, srv, "room_id=r1&token="+tok); got != http.StatusUnauthorized {
		t.Fatalf("status = %d, want 401", got)
	}
}

func TestServeHTTPAcceptsValidTokenAndJoins(t *testing.T) {
	srv := newTestServer(t)
	tok := signToken(t, uuid.NewString(), time.Now().Add(time.Hour))
	url := "ws" + strings.TrimPrefix(srv.URL, "http") + "/ws?room_id=r1&token=" + tok

	conn, _, err := websocket.DefaultDialer.Dial(url, nil)
	if err != nil {
		t.Fatalf("dial with valid token: %v", err)
	}
	defer conn.Close()

	// The server pushes "joined" right after the join; an "offer" may arrive
	// around it, so read until "joined" shows up.
	if err := conn.SetReadDeadline(time.Now().Add(5 * time.Second)); err != nil {
		t.Fatalf("set read deadline: %v", err)
	}
	for {
		var msg Message
		if err := conn.ReadJSON(&msg); err != nil {
			t.Fatalf("waiting for joined: %v", err)
		}
		if msg.Type == "joined" {
			return
		}
	}
}
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `cd /www/my/vycord/server && go test ./internal/sfu/transport/signaling/`
Expected: FAIL — компиляция: `too many arguments in call to NewHandler`.

- [ ] **Step 3: Реализовать проверку в хендлере**

В `server/internal/sfu/transport/signaling/handler.go`:

Импорты — добавить `"github.com/vycord/server/pkg/authtoken"`.

Структура и конструктор:

```go
// Handler is the HTTP handler that upgrades connections to WebSocket and
// drives the per-client signaling lifecycle.
type Handler struct {
	manager   *application.RoomManager
	log       *slog.Logger
	jwtSecret string
}

func NewHandler(manager *application.RoomManager, log *slog.Logger, jwtSecret string) *Handler {
	return &Handler{manager: manager, log: log, jwtSecret: jwtSecret}
}
```

Начало `ServeHTTP` (заменить строки 34–39 текущей версии):

```go
func (h *Handler) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	token := r.URL.Query().Get("token")
	if token == "" {
		http.Error(w, "missing token", http.StatusUnauthorized)
		return
	}
	roomID := r.URL.Query().Get("room_id")
	if roomID == "" {
		http.Error(w, "missing room_id", http.StatusBadRequest)
		return
	}

	uid, err := authtoken.ValidateToken(h.jwtSecret, token)
	if err != nil {
		h.log.Warn("rejected connection: invalid token", "room_id", roomID, "error", err)
		http.Error(w, "invalid token", http.StatusUnauthorized)
		return
	}
	userID := uid.String()
```

Остальное тело `ServeHTTP` не меняется — `userID` дальше используется как раньше.

- [ ] **Step 4: Удалить `JoinPayload`**

В `server/internal/sfu/transport/signaling/protocol.go` удалить целиком неиспользуемый тип (строки 13–16) — не только поле `UserID`, весь struct нигде не используется:

```go
type JoinPayload struct {
	RoomID string `json:"room_id"`
	UserID string `json:"user_id"`
}
```

Комментарий `// --- Client → Server ---` остаётся (под ним `AnswerPayload`, `ICECandidatePayload`).

- [ ] **Step 5: `cmd/sfu/main.go` — `.env` и fail-fast**

В `server/cmd/sfu/main.go` после создания логгера (строка 20) добавить загрузку `.env` (как в `cmd/api/main.go` — иначе `make run-sfu` в деве сломается; в docker env приходит напрямую) и чтение секрета:

```go
	// Load .env like cmd/api does: `make run-sfu` runs from server/ without
	// injected env; in docker the variables come from env_file directly.
	for _, p := range []string{".env", "../.env", "../../.env"} {
		if _, err := os.Stat(p); err == nil {
			if err := godotenv.Load(p); err != nil {
				log.Warn("failed to load .env file", "path", p, "error", err)
			}
			break
		}
	}

	jwtSecret := os.Getenv("JWT_SECRET")
	if jwtSecret == "" {
		log.Error("JWT_SECRET environment variable is required")
		os.Exit(1)
	}
```

Импорты — добавить `"github.com/joho/godotenv"` (уже в go.mod). Строку 44 заменить на:

```go
	handler := signaling.NewHandler(manager, log, jwtSecret)
```

- [ ] **Step 6: Прогнать тесты**

Run: `cd /www/my/vycord/server && go build ./... && go vet ./... && go test ./...`
Expected: сборка ок, PASS — включая 6 новых тестов хендлера и тесты `application`.

- [ ] **Step 7: Commit**

```bash
git add server/internal/sfu/transport/signaling/ server/cmd/sfu/main.go
git commit -m "$(cat <<'EOF'
VYC-25 SFU: JWT-аутентификация на /ws

user_id больше не принимается из query и JoinPayload — идентичность
только из claims токена. Без валидного токена соединение отклоняется
до WebSocket-upgrade. SFU падает на старте без JWT_SECRET и грузит
.env в деве, как cmd/api.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Клиент — токен в URL подключения к SFU

**Files:**
- Modify: `client/src/services/groupCall.ts:642-643` (`connectSignaling`), `client/src/services/groupCall.ts:672-673` (лог `WS connected`)

**Interfaces:**
- Consumes: SFU из Task 3 ожидает `/ws?room_id=...&token=...`; токен лежит в `localStorage['vycord_token']` (кладёт `authStore.ts`).
- Produces: ничего нового для других тасков; сигнатуры `joinGroupCall(roomId, userId)` и `connectSignaling(roomId, userId)` не меняются (`userId` остаётся для логов и фильтрации self-тайла).

- [ ] **Step 1: Переписать формирование URL**

В `client/src/services/groupCall.ts` заменить начало `connectSignaling` (строки 642–643):

```ts
  private connectSignaling(roomId: string, userId: string): Promise<boolean> {
    const url = `${SFU_URL}/ws?user_id=${userId}&room_id=${roomId}`;
```

на:

```ts
  private connectSignaling(roomId: string, userId: string): Promise<boolean> {
    // Read the token on every attempt, not once per call: a VYC-24 reconnect
    // may run after the user re-logged in, and a token frozen at join time
    // would be stale.
    const token = localStorage.getItem('vycord_token') ?? '';
    const url = `${SFU_URL}/ws?room_id=${encodeURIComponent(roomId)}&token=${encodeURIComponent(token)}`;
```

Перечитывание на каждой попытке реконнекта получается автоматически: цикл `reconnect()` зовёт `connect()` → `connectSignaling()` на каждой итерации (строка 263).

- [ ] **Step 2: Убрать токен из лога**

Строка 673 логирует URL целиком — теперь в нём токен. Заменить:

```ts
        gcLog(userId, 'WS connected', { url });
```

на:

```ts
        gcLog(userId, 'WS connected', { roomId });
```

- [ ] **Step 3: Проверить типы**

Run: `cd /www/my/vycord/client && npx tsc --noEmit`
Expected: без ошибок. (Если системный Node < 20 — взять Node 22 из nvm, как делает `e2e/run.sh`.)

- [ ] **Step 4: Commit**

```bash
git add client/src/services/groupCall.ts
git commit -m "$(cat <<'EOF'
VYC-25 Клиент передаёт JWT при подключении к SFU

Токен читается из localStorage на каждой попытке подключения,
чтобы реконнект VYC-24 не использовал замороженный токен.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: E2E — токены для реального SFU

**Files:**
- Modify: `client/e2e/run.sh:52-53` (запуск SFU)
- Modify: `client/e2e/no-camera-screenshare.html:107-109` (id участников), `:176-178` (join-последовательность), плюс новая функция `makeToken`

**Interfaces:**
- Consumes: SFU из Task 3 (требует `JWT_SECRET` на старте, валидирует `?token=`); клиент из Task 4 (читает `localStorage['vycord_token']` в момент connect).
- Produces: ничего.

Контекст: страница гоняет два инстанса `groupCallService` в одном браузерном контексте — localStorage общий. SFU вытесняет stale-сессию того же `user_id` (VYC-24), поэтому у шарера и вьюера должны быть разные токены: joins идут последовательно (`await`), токен подменяется в localStorage перед каждым. `user_id` в claims должен быть валидным UUID — id участников меняются с `sharer-${uuid}` на чистые UUID (сервер теперь рассылает `participant_joined` с id из токена, и он же должен совпадать с ключом `viewerStreams`).

- [ ] **Step 1: run.sh — передать секрет SFU**

В `client/e2e/run.sh` заменить строку 53:

```bash
SFU_PORT="$SFU_PORT" "$WORK_DIR/sfu" >"$WORK_DIR/sfu.log" 2>&1 &
```

на:

```bash
# The SFU refuses to start without JWT_SECRET (VYC-25); the page signs its
# test tokens with the same value via WebCrypto.
JWT_SECRET="e2e-test-secret" SFU_PORT="$SFU_PORT" "$WORK_DIR/sfu" >"$WORK_DIR/sfu.log" 2>&1 &
```

- [ ] **Step 2: Страница — генерация токенов и подмена перед join**

В `client/e2e/no-camera-screenshare.html`:

Заменить строки 107–109:

```js
    const room = `e2e-${Date.now()}`;
    const sharerId = `sharer-${crypto.randomUUID()}`;
    const viewerId = `viewer-${crypto.randomUUID()}`;
```

на:

```js
    const room = `e2e-${Date.now()}`;
    // Must be plain UUIDs: the SFU takes the identity from the token's
    // user_id claim (VYC-25) and rejects non-UUID values.
    const sharerId = crypto.randomUUID();
    const viewerId = crypto.randomUUID();

    // Mirrors run.sh's JWT_SECRET for the test SFU.
    const E2E_JWT_SECRET = 'e2e-test-secret';

    async function makeToken(secret, userId) {
      const enc = new TextEncoder();
      const b64url = (bytes) => btoa(String.fromCharCode(...new Uint8Array(bytes)))
        .replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
      const header = b64url(enc.encode(JSON.stringify({ alg: 'HS256', typ: 'JWT' })));
      const payload = b64url(enc.encode(JSON.stringify({
        user_id: userId,
        exp: Math.floor(Date.now() / 1000) + 3600,
      })));
      const key = await crypto.subtle.importKey(
        'raw', enc.encode(secret), { name: 'HMAC', hash: 'SHA-256' }, false, ['sign'],
      );
      const sig = await crypto.subtle.sign('HMAC', key, enc.encode(`${header}.${payload}`));
      return `${header}.${payload}.${b64url(sig)}`;
    }
```

Заменить строки 176–178:

```js
      log('joining room', room);
      await sharer.joinGroupCall(room, sharerId);
      await viewer.joinGroupCall(room, viewerId);
```

на:

```js
      log('joining room', room);
      // groupCallService reads the token from localStorage at connect time and
      // the two instances share this page's localStorage — set each
      // participant's token right before their (awaited) join. Distinct tokens
      // matter: the SFU evicts a stale session with the same user_id (VYC-24).
      localStorage.setItem('vycord_token', await makeToken(E2E_JWT_SECRET, sharerId));
      await sharer.joinGroupCall(room, sharerId);
      localStorage.setItem('vycord_token', await makeToken(E2E_JWT_SECRET, viewerId));
      await viewer.joinGroupCall(room, viewerId);
```

- [ ] **Step 3: Прогнать e2e**

Run: `cd /www/my/vycord/client && npm run test:e2e`
Expected: `==> result: {...,"pass":true,...}` и `PASS` (занимает 1–2 минуты; при провале логи в указанном `$WORK_DIR`).

- [ ] **Step 4: Финальная проверка всего стека**

Run: `cd /www/my/vycord/server && go test ./... && cd /www/my/vycord/client && npx tsc --noEmit`
Expected: всё PASS, без ошибок типов.

- [ ] **Step 5: Commit**

```bash
git add client/e2e/run.sh client/e2e/no-camera-screenshare.html
git commit -m "$(cat <<'EOF'
VYC-25 E2E подписывает тестовые JWT для SFU

SFU в e2e стартует с JWT_SECRET; страница генерирует токены через
WebCrypto и подменяет их в localStorage перед join каждого участника
(общий localStorage, а SFU вытесняет сессии с одинаковым user_id).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```
