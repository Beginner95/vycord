-- +migrate Down
-- Эта миграция ничего не создаёт сверх того, что уже гарантируют 009 и 011 —
-- она лишь идемпотентно повторяет их бэкфиллы для строк, пропущенных старым
-- бинарём в окне деплоя. Откат не может отличить строку, добавленную этой
-- миграцией, от строки, добавленной обычным CreateServer уже после её наката,
-- поэтому удалять здесь нечего: безопасный no-op.
SELECT 1;
