-- +migrate Up
-- VYC-77: текстовые и голосовые каналы объединены в один универсальный тип.
-- В каждом канале есть и чат, и голос, поэтому колонка type больше не несёт смысла.
ALTER TABLE channels DROP COLUMN type;
